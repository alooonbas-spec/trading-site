import { ModulePage } from "@/components/dashboard/module-page";

export default function SocialAccountsPage() {
  return (
    <ModulePage
      title="Social Accounts"
      description="PHASE 2 adds many accounts per platform, OAuth, account health, groups, and the account selector."
    />
  );
}
