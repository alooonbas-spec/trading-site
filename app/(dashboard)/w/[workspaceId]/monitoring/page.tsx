import { ModulePage } from "@/components/dashboard/module-page";

export default function MonitoringPage() {
  return (
    <ModulePage
      title="Monitoring"
      description="Monitoring rules and events land in PHASE 7 and run only through allowed adapter sources."
    />
  );
}
