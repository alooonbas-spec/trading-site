import { ModulePage } from "@/components/dashboard/module-page";

export default function LeadsPage() {
  return (
    <ModulePage
      title="Leads"
      description="CRM leads, lead status, social profiles, and contact relationships land in PHASE 3. Contact status stays separate from lead status, and do_not_contact stays only on the lead."
    />
  );
}
